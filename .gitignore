# node files
# Note: dist/ is committed in this fork so installs from git work without a build step
# (upstream kisimediaDE/capacitor-live-activity ignores dist/, but we mirror prebuilt artifacts here)
node_modules

# iOS files
Pods
Podfile.lock
Package.resolved
Build
xcuserdata
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/configuration/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.netrc


# macOS files
.DS_Store